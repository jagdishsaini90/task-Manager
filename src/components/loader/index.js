"use client";
import React from "react";
import { LoaderWrapper, Spinner } from "./styles";

const Loader = () => {
  return (
    <LoaderWrapper>
      <Spinner />
    </LoaderWrapper>
  );
};

export default Loader;
